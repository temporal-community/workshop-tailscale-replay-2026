version: 1
sandboxes:
- name: main
  hosts:
  - name: workshop
    type: vm
    image: ubuntu-2404
    memory: 4096
    cpus: 2
    environment:
      # TODO: Replace with actual Tailscale auth key for the workshop tailnet
      TS_AUTHKEY: "tskey-auth-REPLACE-ME"
